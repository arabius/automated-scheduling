# Lesson
```
{
    id : int,
    status: string,
    level : string,
    date : string - yyyy-mm-dd,
    start : string - HH:mm:ss,
    end : string - HH:mm:ss,
    bufferStart : string - HH:mm:ss,
    bufferEnd : string - HH:mm:ss,
    studentGroupHash : string,
    slotId : int,
    branchId : int,
    allowGuide : boolean,
    allowRoom : boolean, 
    alowVideoCall : boolean, 
    enforceRoomCapacity : boolean, 
    requireGuide : boolean, 
    requireRoom : boolean, 
    levelIsDifficult: boolean,
    levelGuideStickiness: int,
    initialGuideId: int
    initialRoomId: int
    allowedGuideIds: string of Ids separated by |
}
```